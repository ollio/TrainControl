<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textColor">#FFFFFF</item>
        <!--<item name="android:background">@color/default_screen_bg</item>-->
        <item name="android:popupBackground">@color/default_screen_bg</item>
    </style>

</resources>
